# ==========================================
# CONFIGURAÇÃO EXEMPLO - Picornavirus-quali2026
# Copie para config/picornavirus.env e ajuste
# ==========================================

# --- AMOSTRA / ENTRADA ---
# ID lógico da amostra (usado em nomes de arquivos)
SAMPLE_ID="amostra_teste"

# Diretório padrão para FASTQ
RAW_DIR="data/raw"

# Leituras opcionais (se quiser apontar para caminhos externos)
# SAMPLE_R1="/caminho/para/leitura_R1.fastq.gz"
# SAMPLE_R2="/caminho/para/leitura_R2.fastq.gz"
# SAMPLE_SINGLE="/caminho/para/leitura_single.fastq.gz"

# --- MONTAGEM ---
# Opções: "spades" ou "velvet"
ASSEMBLER="velvet"

# Threads gerais (SPAdes / BLAST / Bowtie2)
THREADS=4

# Parâmetros do SPAdes
SPADES_PARAMS="--rnaviral -k 21,33,55,77"

# Parâmetros do Velvet
VELVET_K=51
VELVET_OPTS="-exp_cov auto -cov_cutoff auto -min_contig_lgth 200"

# --- BANCO DE DADOS (DB manager) ---
# Nome lógico do banco (prefixo para arquivos)
# Padrão: "ptv" (Teschovirus A)
# Outros exemplos:
#   - "psv" para Sapelovirus A
#   - "evg" para Enterovirus G
DB="ptv"

# Query NCBI (EDirect). Use aspas duplas por fora ao exportar via shell.
# Padrão para Teschovirus:
DB_QUERY="\"Teschovirus\"[Organism]"
# Exemplo para Sapelovirus A:
# DB_QUERY="\"Sapelovirus A\"[Organism]"
# Exemplo para Enterovirus G:
# DB_QUERY="\"Enterovirus G\"[Organism]"

# Arquivos/prefixos padrão do banco
REF_FASTA="data/ref/${DB}.fa"
BLAST_DB="blastdb/${DB}"
BOWTIE2_INDEX="bowtie2/${DB}"

# --- BLAST ---
BLAST_EVALUE="1e-5"
BLAST_MAX_TARGET_SEQS=20
